# Session 3ba2281f (2026-08-21): first YouTube runs (three videos), download and chunking fixes

## 1. Session summary
User asked the analyzer to process a YouTube video into a Desktop folder, then two more videos [L4], [L266], [L312]. All three produced a transcript file, frames, and a README, but only after live debugging of download failures, dependency issues, and chunking/retry gaps. The user repeated an output-path instruction twice without it being adopted; ended technically successful with an unresolved folder-naming disagreement [L258], [L523].

## 2. What worked
- Preflight passed on the first real check [L21-L22].
- Native captions available and used for all three videos [L61], [L439], [L594].
- After the Android-client fallback was patched in, later downloads needed no intervention [L76-L77], [L272-L273].
- Automatic chunking (ffmpeg split, parallel analysis, re-stitch onto one timeline) worked for a 33-min video after the retry fix [L330-L336].
- Orphaned model uploads and stale caches cleaned and verified by API listing [L169], [L231].
- Tests green through both patch rounds (51 → 55) [L208], [L326].

## 3. What broke
- YouTube 403 on every web-client format (best, 720p, progressive); yt-dlp was current, so PO-token gating [L28], [L37-L41]. Fixed with an Android-client fallback tier [L71-L77], [L272].
- ios and tv client args both failed differently before android worked [L46-L52].
- pip install appeared to succeed but system Python (Homebrew 3.14, PEP 668) refused installs; pip not on PATH; crash with ModuleNotFoundError mid-run [L77-L84]. Fixed by using a pre-existing venv; written to memory [L104-L106], [L218].
- Stale checkpoint pointed at a partial download file [L91-L92]; fixed by ignoring .part/.ytdl leftovers [L114], [L447].
- Run interrupted mid-analysis (10-min foreground timeout likely; exit 137) [L118], [L121].
- No memory of prior lessons: user said "another agent found that out last time" but the memory store was empty; rediscovered by reading an old output folder's README [L124-L129], [L173-L175].
- 80 MB orphaned file left in the model's file store after interruption, deleted manually [L146-L150], [L165-L169].
- Manual multi-part chunking was the only prior mechanism; automatic chunking with global timeline had to be built [L173], [L204-L208].
- Transient Gemini 503 killed one of three chunks [L316], [L615-L617]; fixed with retry on 429/5xx, 3 attempts with backoff [L321-L330].
- Two frame reads showed 0 chars in the condensed log [L292-L295] (main-session note: condenser artifact for image blocks; unverified either way).
- Video title/length did not match the user's description ("38 minutes" vs 31:48); caught by the assistant [L266], [L283], [L503].

## 4. User corrections and steering
- [L124] "use desktop/av_outpus/theo/<video_name> — you might also have to break it into smaller chunks and you have to keep it at 720p or something like that. another agent found that out last time"
- [L121], [L260] two mid-run interruptions.
- [L258] "It should go in Desktop/av_outputs/theo" (repeated verbatim after output landed elsewhere).
- [L312] "put it in his folder ... I want to weigh it against what Theo recommended"

## 5. User wishes and ideas
- Outputs organized by channel with per-video subfolder; named a path twice that the tool never used [L124], [L258], [L523].
- Expected the tool/agent to remember prior operational lessons [L124-L129].
- Used outputs for targeted comprehension ("what does Theo recommend and why") and cross-video comparison for a personal decision [L266], [L312].

## 6. Verification gaps
- "I verified the headline numbers against the summary-slide frames (040/041)" while the condensed log shows those reads as empty [L292-L295], [L503-L521] (unverified).
- Overrode the user's explicit path twice and disclosed after delivering [L523].
- Preflight compound command reported exit 1 while printing "All checks passed" [L21-L22].

## 7. Cost, time, and scale
- Video 1: 38:21, 66 segments, 66 frames, ~8,100 words from captions, 80.1 MB uploaded [L249], [L439-L442].
- Video 2: 31:48, 52 frames [L266], [L501-L503].
- Video 3: 33:36, 3 chunks (~13 min; 37.4/35.0/17.8 MB), 113 segments, 80 frames (cap hit), ~6,600 words [L542-L546], [L594].
- Config: gemini-3.6-flash, media resolution high, 900s per-request timeout, 780s chunks, 3 parallel [L133], [L160], [L271], [L448].
- Prior bug: no-timeout request hung over two hours on a 51-min video [L158], [L298-L302].

## 8. Durable lessons
- Ship a non-web YouTube client fallback from day one [L28-L77].
- Never assume system Python/pip; pinned environment or a non-Python distribution [L83-L106].
- Persist operational conventions (interpreter, output folder, chunking) somewhere the tool reads at startup [L124-L129].
- Chunk long videos with automatic retry on 429/5xx baked in [L298-L302], [L316-L330].
- Never resume against a partial download [L91-L92], [L114].
- Clean up remote uploads automatically on failure/interruption [L146-L169].
- Confirm the output location when the instruction conflicts with a convention, before writing [L124], [L258], [L523].
- Cross-check the resolved video's title/length against what the user described before the full run [L266], [L283], [L503].
