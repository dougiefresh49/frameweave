# Session b3037a82 (2026-09-01): genome-data YouTube video, then personal research off the video's linked assets

## 1. Session summary
User asked the analyzer to process a YouTube video into a creator-named Desktop folder [L2-5]. The run succeeded after two failures (download 403s, missing Python package). The rest of the multi-hour session used the video's linked materials (a Drive folder, user's own lab PDFs) for personal research, not the transcript file. Ended satisfied with the analyzer output.

## 2. What worked
- Preflight passed first try [L88].
- Download fallback chain recovered from three consecutive 403s without intervention [L97-99].
- Native YouTube captions used, no Whisper cost [L137].
- Linked Drive folder fetched via gdown (343MB) [L155-161].
- Screenshot/console verification loop caught a real cosmetic bug in the downstream dashboard [L568-576].

## 3. What broke
- YouTube 403 x3 before the Android client fallback [L97-99].
- System python3 lacked the Gemini SDK; crashed mid-pipeline after download; re-run with venv resumed from checkpoint without re-download [L101-119].
- Analyzer nested an extra folder level inside --out-dir (channel/frameweave/slug); manually flattened; "you'll want to flatten that each time" [L124], [L469].
- pdftoppm missing; ad hoc PDF text extraction instead [L317-326].
- Design-tool login blocked in headless session [L352-353] (unrelated).
- Console error present yet "Renders clean with no JS errors" asserted next turn [L452-455].
- WebFetch 403 on one site; WebSearch instead [L278-280].

## 4. User corrections and steering
- "Idk why he uploaded it as a zip file and not on gh." [L147]
- "The genetics digest site is terrible looking and has my internal 'scam alert' radars up. What do you think?" [L234]
- "the atv_outputs is technically supposed to go with the rest of the outputs on the desktop folder... is it troublesome to combine the folders" [L301]
- "the one in the video was kinda just bleh text based. could we throw something into claude.ai/design" [L299]
- Long reframing turn on research direction [L510]

## 5. User wishes and how the transcript was consumed
- The user never queried the transcript file after delivery. Next request was about the video description (a Drive link), which the tool had not captured; the assistant re-fetched it with yt-dlp --print description [L146-151]. Concrete gap: description/metadata/links not in the output.
- Every later turn worked from artifacts the video referenced, not the transcript or frames. The 62 frames were never mentioned again [L130].
- Wanted consolidated creator-grouped Desktop output, not repo-internal [L301], [L469].
- The transcript functioned as a one-time trigger for a summary plus a pointer to linked resources.

## 6. Verification gaps
- "Renders clean" contradicted by the tool's own prior console output [L452-455].
- Nested-output bug patched by hand, never fixed; narrated as fully successful [L124], [L469].
- "Everything's done" [L465] while the requested integration was substituted, not achieved.

## 7. Cost, time, and scale
- Video 22:31, 53 segments, 2 Gemini chunks [L134-135]. 62 frames (cap 80, interval 45s) [L130]. Captions native [L137-138].
- Analyzer run ≈ 3 min wall clock after fixes [L2], [L122]. Session > 9h elapsed.

## 8. Durable lessons
- Honor --out-dir exactly; no hidden nested directory [L124], [L469].
- Bundle/pin the runtime or fail fast with an actionable error; ambient python3 crashed mid-pipeline [L101-119].
- Preflight should cover every load-bearing optional binary [L317].
- Capture video description, chapters, and links as part of the output; the user needed exactly this [L147-151].
- Reconcile "clean" claims against the check's actual output [L452-455].
- Frames went unreferenced after delivery in this session; frame extraction should be cheap, capped, or on demand rather than assumed valuable [L130].
