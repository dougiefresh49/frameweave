# Session abd7e154 (2026-08-25 → 08-27): second YouTube run, frame-interval fix

## 1. Session summary
User asked the analyzer to process a Theo YouTube video into frameweave/theo/ on the Desktop [L3]; the run succeeded (46 segments/frames, transcript file, README, usage log) [L64-L70]. Two days later the user asked about frames missing key on-screen moments [L81], accepted a fix (re-download + a frames patch for evenly-spaced extra frames) [L112], and the session ended with the patch tested, committed as two commits, and pushed to the user's fork [L276-L283]. Ended satisfied.

## 2. What worked
- Convention/memory lookup before running, so output landed in the expected frameweave/<channel>/<slug>/ layout [L8-L9].
- Backgrounded run completed cleanly: 39:28 video → 4 Gemini chunks → 46 segments/frames, native captions [L36], [L67].
- README generation followed the existing folder's shape (claim/evidence table, video map, caption-mangling notes) [L69], [L18].
- YouTube download fallback chain (progressive → Android client) recovered from 403s without user intervention, both runs [L75], [L250].
- Patch + tests landed clean: 65/65 tests passed [L184], [L245].

## 3. What broke
- YouTube 403 on adaptive formats, both downloads. Fallback handled it automatically [L26], [L29], [L75], [L250].
- Frames only captured at segment start, so three stretches (4m41s, 1m25s, 46s) had no frame showing the on-screen content the audio referenced [L93-L99]. Fix: re-download + evenly spaced extra frames for segments longer than --frame-interval (default 45s); 46 → 78 frames [L242], [L232].
- Gemini misread a number: a VISUAL line said "55 memories" where audio and the audit said 45 [L76]. Not corrected in the data, only flagged in the README.
- Download cache deleted after every run, so the frame fix required a full ~80-100MB re-download with no Gemini re-analysis needed [L89], [L250]. Worked around, not fixed.

## 4. User corrections and steering
- [L81] "So is there anything we can do about the missing frames? Or just leave as is?"
- [L112] "Ok I guess go with option 1 with the patch to frames.py"
- [L255] "Yes go ahead and commit and push the changes to my forked repo"
No frustration; calm steering.

## 5. User wishes and ideas
- Wanted the missing-frame problem fixed permanently (a flag) rather than documented as a limitation [L81], [L109], [L112].
- Used the transcript file to grep scene-tag counts and word counts for the README [L47].
- Read specific frame images directly to check what was captured before deciding on a fix [L49-L54], [L88-L93].

## 6. Verification gaps
- "Everything is done and verified" [L227], "key claims are now frame-verified" [L232], while admitting "Extras are evenly spaced, not content-chosen — a screen visible for under ~40s can still fall between them" [L251].
- The Gemini numeric misread (55 vs 45) never corrected at source [L76].
- Re-download cost quoted as ~100 MB then ~80 MB [L89], [L250].

## 7. Cost, time, and scale
- Video 39:28, analyzed as 4 chunks of ~13 min [L36], [L67].
- Gemini: 686k input tokens, gemini-3.6-flash at high resolution [L70].
- Frames: 46 → 78 after patch [L232], [L242]. --frame-interval default 45s.
- Re-download ~80-100MB, zero extra Gemini cost [L89], [L250].
- Tests: 65, 0.29s [L184].

## 8. Durable lessons
- Sample multiple frames per segment by length/interval, not just segment start [L93-L99], [L242].
- Multi-tier download fallback chain from day one; 403s recurred every run [L26], [L75], [L250].
- Keep the source video cached after a run; frame-only rework should not re-download [L89], [L250].
- Cross-check numeric claims between audio transcript and vision description; the 45/55 mismatch was caught only by a human [L76].
- Interval sampling is a heuristic; document where short on-screen content can still be missed [L251].
- Keep the transcript format additive so single-frame consumers keep working when extra frames are added [L243].
- Persist the output-location convention so repeat runs don't re-derive it [L8-L9], [L268].
