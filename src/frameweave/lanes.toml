# Vision lane token and quota coefficients (decision 48).
# Recalibrate with `frameweave lanes recalibrate` and paste the result here.
# Packaged with the wheel (src/frameweave/lanes.toml); not read from the repo tree.

[claude]
a = 3997
b = 2034
c = 200
# percent of the window consumed per 1k tokens
five_hour = 0.0166
seven_day = 0.004

[gemini]
a = 300
b = 1265
c = 200
usd_in_per_1k = 0.0003
usd_out_per_1k = 0.0025

# Codex is never an auto candidate (decision 47). Coefficients exist only so an
# explicit `--vision codex` run can still print a projection row.
# Measured once on gpt-6-astra (decision 47): 20 frames spent 28 points of the
# OpenAI 5-hour (primary) window → 0.147 percent per 1k tokens over ~190k tokens;
# a=4000, b=9582 from that astra run. Not refit since.
[codex]
a = 4000
b = 9582
c = 200
primary = 0.147
secondary = 0.03675
